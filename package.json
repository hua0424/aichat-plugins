{
  "name": "aichat-plugins",
  "version": "0.1.0",
  "private": true,
  "description": "HuLa-Server 与 AI 助手（openclaw 等）的通讯桥接服务 — monorepo",
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --filter @aichat/node dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
